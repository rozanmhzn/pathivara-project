import React from "react";

const BackgroundText = ({ text }) => {
  return <span className="lg:text-[7rem] md:text-6xl text-4xl font-bold text-bgColor ml-7 ">{text}</span>;
};

export default BackgroundText;
